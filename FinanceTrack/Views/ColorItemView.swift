import UIKit

class ColorItemView: UIView {
    var isChecked: Bool = false {
        didSet {
//            self.layer.borderColor = Helper.UIColorFromHex(rgbValue: 0x1DB1C1).cgColor
//            self.layer.borderWidth = 1
            
        }
    }
}
